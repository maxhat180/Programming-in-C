# C Learning Progress

## September 6, 2026

### W3Schools progress

- Worked through the C tutorial up to and including:
  [C Variables - Real-Life Examples](https://www.w3schools.com/c/c_variables_reallife.php)
- Next item in the tutorial: **C Variables - Code Challenge**.

### Local development progress

- Confirmed Visual Studio Code is installed.
- Confirmed GCC is installed for compiling native Windows C programs.
- Created `01_memory_addresses.c` to inspect integer values and addresses.
- Used `printf`, the newline escape sequence `\n`, and the `%p` pointer format.
- Compiled the program from VS Code with `Ctrl+Shift+B`.
- Learned the basic build path: C source code -> GCC -> Windows executable.
- Learned that STM32 development uses a cross-compiler that produces ARM
  firmware instead of a Windows `.exe`.

### Resume point

This initial stopping point was superseded by the September 7 update below.

## September 7, 2026

### Current understanding

- Familiar with the general concepts behind variables, conversions, operators,
  conditions, loops, arrays, and functions.
- Refreshed memory addresses, hexadecimal address notation, pointer types, and
  the purpose of a `void *` pointer.
- Current W3Schools position: **Functions**.

### Learning decision

- Stop treating W3Schools as a required linear curriculum.
- Use it as a syntax reference while working through the local, embedded-focused
  sequence in `PRACTICE_PLAN.md`.
- Next task: `02_TASK.md` and `02_functions_and_pointers.c`.
